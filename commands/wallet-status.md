# wallet-status

Show the current agent wallet status.

## Instructions

1. Call the `sui_wallet_status` MCP tool
2. Display the result in a clear format:
   - Active address
   - Network (CLI env + MCP network)
   - SUI balance
3. If wallet is not configured, suggest running `sui client active-address` to check
